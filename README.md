# svg-scaling-laws

## Setup

```bash
conda env create -f environment.yaml
conda activate svg-scaling-laws
```

## Run Data Processing Pipeline

```bash
python scripts/prepare_data.py --datasets svg-icons-simple --stats
```
